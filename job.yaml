apiVersion: batch/v1
kind: Job
metadata:
  name: customer-orders-sync
spec:
  template:
    spec:
      containers:
        - name: data-sync
          image: data-sync:latest
          imagePullPolicy: Never
          env:
            - name: PG_HOST
              value: "host.minikube.internal"
      restartPolicy: Never